# 네이버 크롤러 수정 내역

## 수정 파일: `src/crawl_naver.py`

---

## [2026-05-28] 커밋 `5ec47e7`

### 배경
- 팀원이 크롤러 실행 시 "found 0개 식당" 출력, 리뷰 수집 불가 현상 발생
- 네이버 지도가 React router로 전환하면서 DOM 구조 변경

---

### 수정 1 — `search_places()`: place_id 추출 방식 교체

**원인:**
- 기존 코드는 `a` 태그 `href`에서 `/restaurant/(\d+)` 패턴으로 place_id 추출
- 네이버 React router 전환으로 모든 `a.href`가 `"#"` 으로 변경됨
- `data-nclick`, `data-id`, `data-place-id` 속성도 존재하지 않음
- 결과: place_id 추출 실패 → 모든 카드 스킵 → 0개 반환

**수정 내용:**
- 카드(`a.YTJkH`) 클릭 → 메인 URL이 `/place/{id}` 형태로 변경됨
- `driver.current_url`에서 `/place/(\d+)` 정규식으로 place_id 추출
- 실패 시 `#entryIframe` src URL에서도 추출 시도
- 스크롤/더보기를 먼저 완료한 후 카드 목록 확정, 이후 클릭 순회

**테스트 결과:** 강남/한식 10개 식당 정상 수집 ✅

---

### 수정 2 — `collect_reviews_and_meta()`: 직접 접근 → map.naver.com 경유

**원인:**
- 기존 코드: `driver.get("https://pcmap.place.naver.com/restaurant/{id}/review/visitor")` 직접 접근
- 네이버 봇 감지로 차단 → "이용이 제한되었습니다" 오류 출력
- `#entryIframe` 경유도 동일하게 차단됨 (IP 레벨 차단)

**수정 내용:**
- `map.naver.com/p/search/{name}/place/{id}?placePath=%2Freview%2Fvisitor` 로 진입
- `#entryIframe` 전환 후 리뷰 수집

**테스트 상태:** ⚠️ IP 차단으로 테스트 미완료 (차단 해제 후 검증 필요)

---

### 수정 3 — `REVIEW_URL` 변경

```
변경 전: https://pcmap.place.naver.com/restaurant/{place_id}/review/visitor
변경 후: https://pcmap.place.naver.com/place/{place_id}/review/visitor
```

- 네이버가 `/restaurant/` → `/place/` 경로로 통합함 (리다이렉트는 되지만 직접 사용)

---

---

## [2026-05-28] 봇 탐지 우회 및 셀렉터 전면 개편

### 배경
- IP `211.221.205.72` 차단으로 `map.naver.com` 경유에도 리뷰 0개 수집
- headless Chrome 자동화 자체를 Naver가 탐지하는 것으로 확인
- DOM 직접 확인으로 실제 리뷰 셀렉터 파악

---

### 수정 1 — `make_driver()`: undetected-chromedriver 전환

- `selenium.webdriver.Chrome` → `undetected_chromedriver.Chrome`으로 교체
- uc가 webdriver property 탐지 및 ChromeDriver 시그니처를 자동으로 우회
- `version_main=148` 고정 (현재 환경 Chrome 148 기준)
- `webdriver` property 수동 override 제거 (uc가 처리)

**설치 필요:**
```bash
pip install undetected-chromedriver setuptools
```

> `setuptools`는 Python 3.12+ 에서 제거된 `distutils` shim 제공용

---

### 수정 2 — `collect_reviews_and_meta()`: 탭 클릭 방식으로 전환

- 기존: `placePath=%2Freview%2Fvisitor` URL 파라미터로 리뷰 탭 직접 호출
- 변경: 플레이스 메인 페이지 진입 후 "방문자 리뷰" 탭을 직접 클릭
- IP 차단 감지 로직 추가 (`"이용이 제한"` 문자열 감지 → early return)
- 콘텐츠 렌더링 대기: `_content_loaded()` WebDriverWait 최대 15초
- 함수 시그니처 변경: `(driver, place)` → `(driver, place, area, category)`

---

### 수정 3 — `search_places()`: StaleElement 완전 해소

- `for card in cards` → 인덱스 기반 순회로 변경 (매 반복 카드 재조회)
- 중복 place_id 스킵 시 searchIframe 재진입 복구
- 성공 후 searchIframe 재진입 복구

---

### 수정 4 — `_collect_visible_reviews()`: 셀렉터 업데이트

DOM 직접 확인(`--show-browser` + F12) 결과:

| 셀렉터 | 용도 |
|--------|------|
| `.pui__vn15t2` | 리뷰 텍스트 컨테이너 (최우선) |
| `.pui__vn15t2 span` | 내부 span |
| `a.pui__GStJHb` | 더보기 버튼 |

- "더보기" / "접기" 텍스트 후처리 제거 (`re.sub`)
- `el.text` StaleElementReferenceException 보호 추가

---

### 수정 5 — `collect_reviews_and_meta()`: rating / review_count 오염 수정

**원인:**
- `rating`: `[class*='ratingValue']` 셀렉터가 "방문자 리뷰 1,068" 텍스트 요소를 잘못 매핑
- `review_count`: `.place_section em` 셀렉터가 가격 요소를 잡아 `16731900` 같은 값 저장

**수정 내용:**
- `rating`: `re.match(r"^\d+(\.\d+)?$", t)` — 순수 숫자(소수점 포함)만 허용
- `review_count`: 셀렉터에서 `.place_section em` 제거, `1 ≤ 값 ≤ 500,000` 범위 검증 추가

---

### 수정 6 — `main()`: `--limit` 인수 추가

```bash
# 식당 3개만 테스트
python src/crawl_naver.py --area 강남 --category 한식 --limit 3
```

---

### 테스트 결과 (2026-05-28, 핫스팟 IP)

```
[search] 강남 / 한식
[found] 3개 식당
[collect] 1/3 육달집 강남본점     → 리뷰 25개 수집 ✅
[collect] 2/3 사계절집 강남본점   → 리뷰 16개 수집 ✅
[collect] 3/3 봉계타 무역센터점   → 리뷰 40개 수집 ✅
총 리뷰: 81개
```

**확인 완료 사항:**
- [x] IP 차단 해제 (핫스팟 사용)
- [x] 리뷰 수집 정상 동작
- [x] rating / review_count 오염 수정
- [x] 더보기 / 접기 텍스트 제거

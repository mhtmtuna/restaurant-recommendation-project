# 망고플레이트 크롤링 가이드

망고플레이트에서 리뷰를 추가 수집하고 기존 데이터에 병합하는 방법입니다.  
카카오맵보다 "데이트 코스로 딱이에요", "회식 자리로 좋아요" 같은 관계 맥락 표현이 많아 모델 F1 개선이 목표입니다.

---

## 0. 사전 준비

```bash
# 프로젝트 폴더로 이동
cd AI_TeamProject_recommendation

# 패키지 설치 (이미 했으면 생략)
pip install -r requirements.txt
```

Chrome 브라우저가 설치되어 있어야 합니다.  
ChromeDriver는 selenium 4.6+ 기준 자동 설치됩니다.

---

## 1. 크롤링 실행

### 전체 실행 (강남·건대·잠실 × 11개 카테고리)

```bash
python src/crawl_mangoplate.py
```

- 기본값: 헤드리스 모드 (브라우저 창 안 뜸)
- 중단 후 재시작해도 `crawl_status_mangoplate.csv` 기준으로 이어서 진행
- 결과: `data/raw_reviews_mangoplate.csv`

### 브라우저 보면서 실행 (디버깅 용)

```bash
python src/crawl_mangoplate.py --show-browser
```

### 특정 지역/카테고리만

```bash
python src/crawl_mangoplate.py --area 강남
python src/crawl_mangoplate.py --area 잠실 --category 한식
```

---

## 2. 진행 상황 확인

크롤링 중 터미널에 실시간으로 출력됩니다:

```
[search] 강남 / 한식
[found] 80개 식당
[collect] 1/80 OO식당
  → 리뷰 40개 수집
[collect] 2/80 XX식당
  → 리뷰 23개 수집
...
```

중단했다가 다시 실행하면 `[skip]`으로 이미 완료된 식당은 건너뜁니다.

---

## 3. 기존 데이터와 병합

크롤링 완료 후:

```bash
python src/merge_reviews.py
```

`data/raw_reviews.csv`(기존 카카오) + `data/raw_reviews_mangoplate.csv`(신규)를  
하나의 `data/raw_reviews.csv`로 합칩니다. 중복 리뷰는 자동 제거됩니다.

> **주의:** 기존 `raw_reviews.csv`를 덮어씁니다.  
> 백업이 필요하면 먼저 `data/raw_reviews_backup.csv`로 복사해두세요.

---

## 4. 모델 재학습

병합 완료 후 현성이에게 전달하거나, 직접 실행:

```bash
python src/build_features.py
python src/train_model.py
```

---

## 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| `[warn] 검색 결과 없음` | 망고플레이트 UI 변경 or 네트워크 | `--show-browser`로 실제 페이지 확인 |
| 리뷰가 0개로 수집됨 | 리뷰 탭 CSS 선택자 변경 | `crawl_mangoplate.py`의 `_collect_visible_reviews()` 선택자 수정 |
| ChromeDriver 오류 | Chrome 버전 불일치 | `pip install --upgrade selenium` |
| 크롤링 중 멈춤 | 망고플레이트 로딩 지연 | 그냥 기다리거나 재시작 |

---

## 파일 구조

```
data/
├── raw_reviews.csv               ← 최종 병합 파일 (모델 학습에 사용)
├── raw_reviews_mangoplate.csv    ← 망고플레이트 수집 결과
├── crawl_status_mangoplate.csv   ← 진행 상태 (재시작 기준)
└── crawl_errors_mangoplate.csv   ← 에러 로그
```

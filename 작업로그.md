# AI 추천시스템 작업 로그

**프로젝트 경로:** `C:\Users\frado\가천대\인공지능프로그래밍\팀플\AI_TeamProject_recommendation`
**데이터:** 807개 식당, 27,091개 리뷰 (2026-05-27 기준)

---

## 기준선 (Before A1)

> 2026-05-26 측정 — sample 데이터 기준 176개 식당

| 라벨 | Positive 수 | F1 | 비고 |
|------|------------|-----|------|
| couple_meal | 6 | 0.00 | |
| couple_drink | 6 | 0.00 | couple_meal과 키워드 완전 동일 |
| friend_meal | 29 | 0.25 | |
| friend_drink | 11 | 0.00 | |
| business_meal | 8 | 0.00 | |
| business_drink | 0 | 0.00 | 키워드가 실제 리뷰에 미출현 |
| **macro F1** | — | **0.042** | |

---

## 완료된 작업

### ✅ A1. 키워드 분리 (2026-05-26)

**문제:**
1. `couple_meal`과 `couple_drink`가 완전히 동일한 키워드 7개 → 모델이 두 라벨 구별 불가
2. `business_drink` positive 0개 → 복합 구문 키워드("회식 자리", "거래처 술자리")가 실제 카카오맵 리뷰에 거의 미출현
3. `friend_meal`/`friend_drink`에 "같이", "다같이", "편하게" 등 과도하게 범용적인 단어 포함

**변경 내용 (`config/keywords.json`):**

| 라벨 | 변경 사항 |
|------|---------|
| `couple_meal` | 기존 7개 유지 + 식사 특화 키워드 추가 (기념일 저녁, 여친이랑 밥 등) |
| `couple_drink` | 기존 7개 유지 + 음주 특화 키워드 추가 (여친이랑 술, 데이트 한잔 등) → 더 이상 identical 아님 |
| `friend_meal` | "같이", "다같이", "워라밸", "편하게" 등 범용어 제거, 구체적 맥락어 유지 |
| `friend_drink` | "2차", "술자리", "뒤풀이" 등 음주 맥락어 강화 |
| `business_meal` | 소폭 확장 (직장 동료, 동료들이랑 추가) |
| `business_drink` | 복합 구문 → 단순 키워드 ("회식", "팀 회식" 등)로 교체 |

**결과 (477개 식당 전체 데이터):**

| 라벨 | Positive 수 (변화) | F1 | 비고 |
|------|-------------------|-----|------|
| couple_meal | 20 (+14) | 0.00 | |
| couple_drink | 20 (+14) | 0.00 | 구조적으로 분리됨 |
| friend_meal | 30 (+1) | 0.00 | |
| friend_drink | 36 (+25) | **0.05** | 첫 신호 감지 |
| business_meal | 15 (+7) | 0.00 | |
| business_drink | 11 (**+11**) | 0.00 | 0개 → 11개 복구 |
| **macro F1** | — | 0.008 | 전체 데이터 기준 재측정 |

**추가 발견 — `price` 컬럼 전체 NULL:**
`sklearn` imputer 경고: `Skipping features without any observed values: ['price']`
→ 크롤링 시 price 필드가 수집되지 않고 있음. 크롤러 점검 필요.

---

## 완료된 추가 작업

### ✅ 피처 재생성 + 모델 재학습 (2026-05-27)

**실행 명령:**
```bash
python src/build_features.py
python src/train_model.py
```

**데이터:** 807개 식당, 27,091개 리뷰 반영

**라벨별 결과 (OOF 5-fold, couple 통합 후 최종):**

| 라벨 | Positive(전→후) | F1(전→후) | 데이터 충분 여부 |
|------|----------------|-----------|----------------|
| couple | 23 → **25** | 0.00 → **0.18** | ❌ (권장 30+) |
| friend_meal | 35 → **47** | 0.00 → **0.18** | ✅ |
| friend_drink | 42 → **61** | 0.04 → **0.27** | ✅ |
| business_meal | 15 → **17** | 0.00 → **0.15** | ❌ (권장 30+) |
| business_drink | 11 → **13** | 0.00 → **0.10** | ❌ (권장 30+) |
| **macro F1** | | **0.007 → 0.176** | |

**주의사항:**
- `price` 컬럼 전체 NULL 경고 지속 — 크롤러에서 price 미수집 중
- ~~fold 5가 train=75 / val=732으로 극단적 불균형~~ → **수정 완료 (아래 참고)**

### ✅ fold 불균형 버그 수정 + A3 Threshold Tuning (2026-05-27)

**버그:** `multilabel_fold_indices`에서 레이블 없는 음성 샘플이 전부 마지막 fold에 몰림
- 수정 전: fold 5 → train=75 / val=732 (비정상)
- 수정 후: 각 fold → train≈645 / val≈162 (정상)

**원인:** 음성 샘플의 `fold_key`가 `fold_label_counts.sum()`을 기준으로 하여 한 fold에 계속 쌓임
→ 음성 샘플은 `fold_sizes`만으로 분배하도록 수정 (`src/train_model.py`)

**A3 Threshold Tuning 통합:**
- `tune_thresholds()` 함수 추가 — OOF 점수에서 라벨별 최적 threshold 탐색 (0.05~0.90 sweep)
- `model_report.json`에 `optimal_thresholds` 필드 추가

**최종 결과 (OOF, tuned thresholds):**

| 라벨 | Positive | Threshold | P | R | F1 |
|------|----------|-----------|-----|-----|------|
| couple_meal | 25 | 0.10 | 0.12 | 0.56 | **0.19** |
| couple_drink | 25 | 0.10 | 0.12 | 0.56 | **0.19** |
| friend_meal | 47 | 0.15 | 0.14 | 0.40 | **0.21** |
| friend_drink | 61 | 0.25 | 0.26 | 0.33 | **0.29** |
| business_meal | 17 | 0.15 | 0.11 | 0.24 | **0.15** |
| business_drink | 13 | 0.15 | 0.09 | 0.15 | **0.11** |
| **macro F1** | | | | | **0.19** |

> 이전 macro F1: 0.007 (fold 버그 + threshold=0.5 고정) → **0.19 (버그 수정 + tuning)**

---

### ✅ 데이터 확장 (완료 — 2026-05-26)

**변경:** `sampling_plan.json` — `restaurants_per_group`: 35 → 80

| 항목 | 시작 | 현재 (2026-05-27 기준) |
|------|------|------|
| 식당 수 | 477 | **807** (+330) |
| 리뷰 수 | 22,539 | **27,091** (+4,552) |
| 목표 | — | 최대 2,640개 (3×11×80) |

**2026-05-27 크롤 결과:**

| 지역 | 리뷰 수 |
|------|--------|
| 강남 | 10,559 |
| 건대 | 9,457 |
| 잠실 | 7,075 |

| 카테고리 | 리뷰 수 |
|---------|--------|
| 한식 | 3,630 |
| 중식 | 3,443 |
| 일식 | 3,112 |
| 양식 | 3,238 |
| 술집 | 2,915 |
| 치킨 | 2,221 |
| 분식 | 2,179 |
| 고깃집 | 2,151 |
| 카페 디저트 | 1,731 |
| 이자카야 | 1,609 |
| 호프/통닭 | 862 |

**crawl_status 현황:**
- completed: 313개
- partial: 494개 (리뷰 수 부족 — 재시도 가능)
- no_reviews: 38개

크롤링 완료 후 피처 & 모델 재생성 필요:
```bash
python src/build_features.py
python src/train_model.py
```

---

### ✅ A2. 죽은 코드 제거 확인 (2026-05-27)
- `fillna(median())` 적용 확인 ✅
- `OneVsRestClassifier` 미사용, `RandomForestClassifier` native multi-output 사용 중 확인 ✅
- 별도 코드 수정 불필요 — 이미 반영 상태.

---

### ✅ A3. Threshold Tuning (2026-05-27)
- label별 optimal threshold 탐색 (0.05~0.90 sweep) — `tune_thresholds()` 함수로 `train_model.py`에 통합
- 결과를 `model_report.json`의 `optimal_thresholds` 필드에 저장
- macro F1 0.007 → 0.19

---

### ✅ A4. recommend.py area 매칭 수정 (2026-05-26)
- `scores["area"] == args.area` → 부분 문자열 매칭으로 변경
- "강남" 입력 시 "강남구", "강남역" 모두 매칭
- 매칭 결과 0개면 사용 가능한 지역 목록 출력

---

### ✅ A5. README 보완 (2026-05-26)
- "추천 점수 출처 구조" 섹션 추가 (OOF vs 최종 모델 하이브리드 설명)
- "현재 한계와 개선 방향" 테이블 추가

---

### ✅ B1. 다중 모델 비교 (2026-05-27)

`experiments/compare_models.py` 생성, `experiments/model_comparison.json` / `model_comparison.png` 저장

| 모델 | macro F1 | 학습시간 |
|------|----------|---------|
| **RandomForest** | **0.191** | 4.7s |
| CatBoost | 0.169 | 8.1s |
| XGBoost | 0.163 | 2.3s |
| LightGBM | 0.148 | 3.2s |

**라벨별 F1:**

| 라벨 | RF | XGB | CatBoost | LGBM |
|------|-----|-----|---------|------|
| couple_meal | 0.19 | 0.14 | 0.18 | 0.16 |
| couple_drink | 0.19 | 0.14 | 0.18 | 0.16 |
| friend_meal | 0.21 | 0.29 | 0.20 | 0.26 |
| friend_drink | 0.29 | 0.25 | 0.25 | 0.25 |
| business_meal | 0.15 | 0.12 | 0.16 | 0.05 |
| business_drink | 0.11 | 0.06 | 0.05 | 0.00 |

**결론:** RF가 macro F1 기준 1위, 속도도 적절. XGBoost는 friend 라벨에서 강점. business_drink는 전 모델 고전.

---

### ✅ B2. Feature Importance 분석 (2026-05-27)

`experiments/feature_importance.py` 생성, `experiments/feature_importance.png` / `feature_importance.json` 저장

**카테고리별 기여도:**

| 카테고리 | 기여도 |
|---------|--------|
| atmosphere (noise/brightness/spaciousness) | **56.5%** |
| metadata (rating/review_count 등) | 28.8% |
| taste/value/portion | 12.7% |
| location/seat | 2.0% |

**전체 top-5 features:**

| 순위 | feature | importance |
|------|---------|------------|
| 1 | noise_mentions | **0.405** |
| 2 | rating | 0.185 |
| 3 | brightness_mentions | 0.160 |
| 4 | taste_mentions | 0.120 |
| 5 | collected_review_count | 0.048 |

**인사이트:**
- noise_mentions (0.405)가 압도적 1위 — 분위기(조용함/활기참) 언급 빈도가 상황 구분에 핵심
- *_score보다 *_mentions가 중요 — 언급량 자체가 식당 성격을 드러냄
- price 컬럼 전체 NULL이지만 importance 0.005 (다른 피처로 보완되는 중)

---

### ✅ B3. Baseline 비교 (2026-05-27)

`experiments/baseline_comparison.py` 생성, `experiments/baseline_comparison.png` / `baseline_comparison.json` 저장

| 모델 | macro F1 | 비고 |
|------|----------|------|
| Majority (all-zero) | 0.000 | 항상 0 예측 |
| Random | 0.034 | positive rate 비율로 무작위 예측 |
| Rating-only (≥4.0) | 0.082 | 별점 기준 단순 필터 |
| **RF + Threshold (ours)** | **0.191** | |
| Keyword-only | 1.000 | 라벨 = 키워드 규칙이므로 trivially 1.0 (비교 불가) |

**결론:** RF가 Rating-only 대비 2.3배, Random 대비 5.6배 높은 F1. "별점만으로는 상황별 추천 불가" 발표 방어에 사용 가능.

---

### ✅ B6. Human Evaluation 템플릿 생성 (2026-05-27)

`experiments/human_eval.py` 생성, `experiments/human_eval_template.csv` 저장

- **18개 조건** (3 지역 × 6 상황) × top-5 = **90행** CSV 템플릿 생성
- 팀원이 `적절성_1~5` + `평가자` + `메모` 열 작성 후 `human_eval_filled.csv`로 저장
- 집계: `python experiments/human_eval.py --summarize` 실행 시 조건별 평균점수 출력 + `human_eval_summary.csv` 저장

**팀원 할 일:**
1. `experiments/human_eval_template.csv` 열기
2. `적절성_1~5` 열에 1(매우 부적절) ~ 5(매우 적절) 입력
3. `experiments/human_eval_filled.csv`로 저장
4. `python experiments/human_eval.py --summarize` 실행

---

### ✅ couple positive sample 개선 — 간접 키워드 확장 + 피처 기반 규칙 (2026-05-27)

**문제:** couple label positive가 25개로 권장(30개) 미달 — 명시적 표현("여친이랑" 등)이 리뷰에 적음

**개선 1 — 간접 키워드 추가 (`config/keywords.json`):**
- 추가: `로맨틱`, `야경`, `프로포즈`, `소중한 사람`, `특별한 날`, `분위기 있`, `감성 있`, `인스타 감성`, `무드 있`
- 효과: +3개 식당 신규 매칭

**개선 2 — 피처 기반 보완 규칙 (`src/build_features.py`, `apply_couple_feature_rule`):**
- 조건: 2인석/창가 자석 있음 AND (noise_score < 0.45, mentions≥2) OR (brightness_score < 0.45, mentions≥2)
- 효과: +1개 식당 신규 매칭

**결과:**

| 항목 | 전 | 후 |
|------|----|----|
| couple positive | 25 | **29** |
| couple F1 | 0.18 | 0.20 |
| macro F1 | 0.176 | 0.168 |

> **유의미한 개선 아님.** +4개 positive는 OOF 분산 범위 내 변화고, 30개 미달 상태도 동일. 근본 원인(카카오맵 리뷰에 관계 맥락이 잘 명시되지 않음)은 해결되지 않음. 발표 한계점 항목으로 기록.

---

### ✅ couple_meal + couple_drink 통합 → `couple` 단일 라벨

**근거:** 두 라벨이 positive 수(25개), F1(0.19), threshold(0.10) 모두 동일 → 실제로 같은 식당을 가리키고 있음. 카카오맵 리뷰 특성상 meal/drink 구별 없이 "데이트/연인" 키워드를 사용하기 때문.

**변경 내용:**
- `config/keywords.json`: couple_meal + couple_drink 키워드 합집합 → `couple` 단일 라벨
- `src/train_model.py`: LABEL_COLUMNS 6개 → 5개
- `src/recommend.py`: 연인+식사, 연인+술자리 모두 `couple_score` 참조
- `experiments/human_eval.py`: 18조건 → 15조건 (연인 식사/술자리 통합)

**재학습 결과 (5개 라벨 기준):**

| 라벨 | Positive | Threshold | F1 |
|------|----------|-----------|-----|
| couple | 25 | 0.10 | 0.18 |
| friend_meal | 47 | 0.15 | 0.18 |
| friend_drink | 61 | 0.20 | 0.27 |
| business_meal | 17 | 0.20 | 0.15 |
| business_drink | 13 | 0.15 | 0.10 |
| **macro F1** | | | **0.176** |

### ✅ Human Evaluation 진행

팀원이 `experiments/human_eval_template.csv` 작성 후 `human_eval_filled.csv`로 저장.
→ `python experiments/human_eval.py --summarize` 로 집계

### ✅ 발표 범위 확정

스토리 중심으로 구성:
1. 문제 정의
2. 데이터 수집 (807개 식당, 27,091개 리뷰)
3. 피처 설계
4. **B3** — "별점만으로는 부족하다" (Rating-only 대비 RF 2.3배)
5. 모델 결과 + **B2** — noise_mentions이 핵심 피처
6. **B6** — 실제 추천 품질 (Human Eval 결과)
7. 한계 및 개선 방향

> B1 모델 비교는 질의응답 백업 슬라이드로 준비

---

### ✅ web_app.py 자연어 파싱 버그 2건 수정 (2026-05-27)

**Bug 1 — 인원 수 오인식 (여러 N명 표현 → 합산 오류)**

원인: `parsePartySize` fallback 로직이 텍스트 내 모든 N명을 찾아 합산
- "총 남자 4명이서": `총\s*(\d+)\s*명` 패턴이 중간에 "남자"가 있어 매칭 실패 → fallback에서 "3명"(3)+"4명"(4)=7
- "남자 3명 아니다 남자 4명이서": 정정 표현도 동일하게 합산되어 7

수정 1: `총[^0-9]*(\d+)\s*명` — "총" 이후 숫자 외 임의 문자를 건너뛰도록 변경 (early return)
수정 2: 여러 N명 매칭 시 합산 → **마지막 숫자 사용**으로 변경
- 정정 케이스("3명 아니다 4명") → 마지막 4 ✓
- 남자+여자 케이스("남자 3명이랑 여자 2명이랑") → genderPattern이 먼저 캐치하므로 영향 없음 ✓

**Bug 2 — 술집/이자카야/호프/통닭 카테고리 필터 불일치**

원인: UI 드롭다운 옵션은 "주점 바"이나 실제 `restaurants_features.csv`의 카테고리 값은 "술집", "이자카야", "호프/통닭"
- `item.category === "주점 바"` 항상 false → 드롭다운 선택 시 결과 없음
- "상관없음"에서도 friend_drink_score가 낮아 top 10에 미진입

수정: `matchesCategory()` 헬퍼 함수 추가
```js
const DRINK_CATEGORIES = ["술집", "이자카야", "호프/통닭"];
function matchesCategory(item, category) {
  if (category === "상관없음") return true;
  if (category === "주점 바") return DRINK_CATEGORIES.includes(item.category);
  return item.category === category;
}
```

### ✅ web_app.py couple 라벨 통합 누락 수정 (2026-05-27)

**Bug:** `연인_식사` / `연인_술자리` 결과 전부 0점
- 원인: couple 통합 시 `train_model.py`만 수정하고 `web_app.py`는 미반영
- CSV 컬럼은 `couple_score`인데 web_app.py는 `couple_meal_score` / `couple_drink_score`를 참조 → `to_float()` 항상 0.0 반환

**수정 (3군데):**
- `LABEL_COLUMNS_LIST`: `couple_meal`, `couple_drink` → `couple`
- `LABEL_COLUMNS` (Python): 연인_식사/술자리 모두 `couple_score`로 매핑
- `labelMap` (JavaScript): 동일하게 `couple_score`로 매핑

---

### ✅ parsePartySize 인원 합산 오류 추가 수정 (2026-05-27)

**Bug:** "남자 3명 아니다 남자 4명이서"처럼 정정 표현 시 3+4=7로 합산
- 수정: 여러 N명 매칭 시 합산 → **마지막 숫자 사용** (`numberMatches[last]`)
- 남자+여자 케이스("남자 3명이랑 여자 2명이랑")는 genderPattern이 먼저 캐치하므로 영향 없음

---

### ✅ occasion 파싱 키워드 누락 수정 (2026-05-27)

**Bug 1:** "연인 잠실 카페" 입력 시 상황이 "술자리"로 유지됨
- 원인: "카페" 키워드가 occasion 파싱 키워드에 없어 이전 값("술자리") 잔존
- 수정: 식사 occasion 키워드에 `"카페"`, `"커피"`, `"디저트"` 추가

**Bug 2:** "이자카야", "술집", "호프" 등 술집 장소 키워드 입력 시 상황이 "술자리"로 인식 안 됨
- 수정: 술자리 occasion 키워드에 `"술집"`, `"주점"`, `"이자카야"`, `"호프"`, `"맥주집"`, `"와인바"` 추가

---

### ✅ 연인 입력 시 성비 "남자 위주" 오설정 수정 (2026-05-27)

**Bug:** "연인 잠실 술집"처럼 입력 시 성비가 이전 입력값("남자 위주") 그대로 유지됨
- 원인: "연인" 파싱 시 genderMix를 설정하지 않아 이전 드롭다운 값이 잔존
- 수정: "연인/데이트/애인" 감지 시 `parsed.genderMix = "반반"` 자동 설정
  - 이후 명시적 성별 표현이 있으면 성비 파싱 블록에서 덮어쓰므로 충돌 없음

---

### ✅ 지역 미인식 경고 문구 불일치 수정 (2026-05-27)

**Bug:** 지역 키워드 없을 때 경고 "기본값(강남)이 적용됩니다"라고 표시하나, 실제로는 드롭다운 이전 값(예: 잠실)이 유지됨 → 경고와 동작 불일치

**수정:** 경고 문구를 실제 동작에 맞게 변경
- `"기본값(강남)이 적용됩니다."` → `"현재 선택된 지역이 유지됩니다."`
- 드롭다운을 강남으로 강제 리셋하지 않는 것이 올바른 UX (사용자가 직접 고른 지역 보존)

---

### ✅ 네이버 크롤러 place_id 추출 방식 수정 (2026-05-28, 커밋 5ec47e7)

**문제:** 크롤러 실행 시 "found 0개 식당" 출력
- 원인: 네이버 지도가 React router로 전환 → 모든 `a.href`가 `"#"` 으로 변경되어 기존 href 파싱 불가

**수정:**
- `search_places()`: 카드(`a.YTJkH`) 클릭 → `driver.current_url`에서 `/place/(\d+)` 정규식으로 place_id 추출
- `collect_reviews_and_meta()`: `pcmap.place.naver.com` 직접 접근(IP 차단) → `map.naver.com/p/search/{name}/place/{id}` 경유로 변경
- `REVIEW_URL`: `/restaurant/` → `/place/` 경로 변경

> ⚠️ 리뷰 수집 부분은 IP 차단 해제 후 셀렉터 동작 검증 필요

---

### ✅ model_training_common 버그 수정 + 모델 재학습 (2026-05-28, 커밋 d49770d)

**문제:** goldblin 커밋 이후 `model_report.json` 충돌 마커로 JSON 파싱 불가, `restaurant_label_scores.csv`가 347개 기준으로 덮어써짐

**수정:**
- `model_training_common.py`: LABEL_COLUMNS `couple_meal`/`couple_drink` → `couple` 통합(5라벨)으로 수정
- `model_training_common.py`: `fold_key` 버그 수정 — 레이블 없는 샘플 729개가 fold 5 하나에 몰리던 문제 → fold_sizes 기준 균등 분배로 변경
- `train_model.py`: 구버전 중복 코드(dead code) 제거
- `model_report.json` / `restaurant_label_scores.csv` / `model_comparison.json`: 807개 기준으로 재생성

**현재 모델 성능 (807개, couple 통합):**

| 라벨 | Positive | F1 |
|------|----------|----|
| couple | 29 | 0.00 |
| friend_meal | 47 | 0.00 |
| friend_drink | 61 | 0.03 |
| business_meal | 17 | 0.00 |
| business_drink | 13 | 0.00 |
| **macro F1** | | **0.006** |

> ⚠️ couple 통합 이후 positive sample 부족(29개)으로 성능 저하는 기존 확인된 한계 사항

---

## 권장 다음 작업 순서

```
✅ 데이터 확장 → build → train
✅ A3 Threshold Tuning
✅ B1 모델 비교 + B2 Feature Importance
✅ B3 Baseline + B6 Human Eval 템플릿
✅ couple 통합, 발표 범위 확정
✅ web_app.py 자연어 파싱 버그 4건 수정
[팀원] Human Eval CSV 작성 → --summarize 실행
[팀원] 발표 슬라이드 제작
```

---

## 참고: keywords.json 교집합 검증

features 키워드 SET ∩ labels 키워드 SET = **공집합** ✅
(부분문자열 포함 주의: "맛집" feature vs "맛집 탐방" label 제거 완료)

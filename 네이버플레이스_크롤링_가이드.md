# 네이버 플레이스 크롤링 가이드

네이버 플레이스 방문자 리뷰를 추가 수집하고 기존 카카오맵 데이터와 병합하는 방법입니다.  
카카오맵보다 "데이트로 왔어요", "친구들이랑", "회식 자리" 같은 관계 맥락 표현이 많아  
모델 F1 점수 개선이 목적입니다.

---

## 0. 사전 준비

```bash
# 프로젝트 폴더로 이동
cd AI_TeamProject_recommendation

# 패키지 설치 (이미 했으면 생략)
pip install -r requirements.txt
```

Chrome 브라우저가 설치되어 있어야 합니다.  
ChromeDriver는 selenium 4.6+ 기준 자동 설치됩니다.

---

## 1. 크롤링 실행

### 전체 실행 (강남·건대·잠실 × 11개 카테고리)

```bash
python src/crawl_naver.py
```

- 기본: 헤드리스 모드 (브라우저 창 안 뜸)
- 중단 후 재시작해도 `crawl_status_naver.csv` 기준으로 이어서 진행
- 결과: `data/raw_reviews_naver.csv`

### 브라우저 보면서 실행 (처음 실행 시 권장)

```bash
python src/crawl_naver.py --show-browser
```

> ⚠️ **처음 실행할 때는 `--show-browser`로 먼저 확인하세요.**  
> 네이버가 UI를 업데이트하면 리뷰가 0개로 수집될 수 있습니다.  
> 브라우저가 뜨면 리뷰 텍스트가 제대로 잡히는지 육안으로 확인하세요.

### 특정 지역/카테고리만

```bash
python src/crawl_naver.py --area 강남
python src/crawl_naver.py --area 잠실 --category 한식
```

---

## 2. 진행 상황 확인

```
[search] 강남 / 한식
[found] 80개 식당
[collect] 1/80 OO식당 (nv_12345678)
  → 리뷰 40개 수집
[collect] 2/80 XX식당 (nv_87654321)
  → 리뷰 18개 수집
[skip] 3/80 ZZ식당   ← 이미 완료된 식당
...
```

---

## 3. 기존 데이터와 병합

크롤링 완료 후:

```bash
python src/merge_reviews.py
```

`data/raw_reviews.csv`(기존 카카오) + `data/raw_reviews_naver.csv`(신규)를  
하나의 `data/raw_reviews.csv`로 합칩니다. 중복 리뷰는 자동 제거됩니다.

> **주의:** 기존 `raw_reviews.csv`를 덮어씁니다.  
> 백업이 필요하면 먼저 따로 복사해두세요.

---

## 4. 모델 재학습

병합 완료 후 현성이에게 전달하거나, 직접 실행:

```bash
python src/build_features.py
python src/train_model.py
```

---

## 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| 리뷰 0개로 수집됨 | 네이버 UI 변경으로 CSS 선택자 불일치 | `--show-browser`로 확인 후 `_collect_visible_reviews()` 선택자 수정 |
| `searchIframe` 없음 | 검색 결과 로드 지연 | 잠시 후 재시도 또는 `--show-browser` 확인 |
| place_id 추출 실패 | 네이버 URL 구조 변경 | `search_places()` 내 `extract_place_id` 정규식 확인 |
| ChromeDriver 오류 | Chrome 버전 불일치 | `pip install --upgrade selenium` |
| 크롤링 도중 멈춤 | 네이버 로딩 지연 | 그냥 기다리거나 중단 후 재시작 (이어서 진행됨) |

---

## 파일 구조

```
data/
├── raw_reviews.csv              ← 최종 병합 파일 (모델 학습에 사용)
├── raw_reviews_naver.csv        ← 네이버 수집 결과
├── crawl_status_naver.csv       ← 진행 상태 (재시작 기준)
└── crawl_errors_naver.csv       ← 에러 로그
```

---

## 참고: 수집 대상 카테고리

| 카테고리 | 검색 키워드 |
|---------|-----------|
| 한식 | 한식 |
| 일식 | 일식 |
| 중식 | 중식 |
| 양식 | 양식 |
| 술집 | 술집 |
| 치킨 | 치킨 |
| 분식 | 분식 |
| 이자카야 | 이자카야 |
| 고깃집 | 고깃집 |
| 호프/통닭 | 호프 |
| 카페 디저트 | 카페 |

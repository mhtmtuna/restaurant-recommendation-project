# 네이버 플레이스 크롤링 가이드

네이버 플레이스 방문자 리뷰를 추가 수집하고 기존 카카오맵 데이터와 병합하는 방법입니다.  
카카오맵보다 "데이트로 왔어요", "친구들이랑", "회식 자리" 같은 관계 맥락 표현이 많아  
모델 F1 점수 개선이 목적입니다.

---

## 전체 흐름

```
팀원 A (강남 담당)  ─┐
팀원 B (건대 담당)  ─┼─ 각자 수집 후 raw_reviews_naver.csv → 디스코드 업로드
팀원 C (잠실 담당)  ─┘

현성이 ─ 파일 3개 수집 → 병합 → 모델 재학습
```

---

## 팀원 작업 (수집 담당)

### 0. 사전 준비

```bash
# 프로젝트 폴더로 이동
cd AI_TeamProject_recommendation

# 패키지 설치
pip install -r requirements.txt
pip install undetected-chromedriver setuptools
```

- Chrome 브라우저가 설치되어 있어야 합니다.
- `undetected-chromedriver`가 ChromeDriver를 자동으로 관리합니다 (별도 설치 불필요).
- `setuptools`는 Python 3.12+ 환경에서 필요한 `distutils` shim입니다.

> ⚠️ **Chrome 버전 확인 필요**  
> `src/crawl_naver.py` 내 `make_driver()`의 `version_main` 값이 설치된 Chrome 버전과 일치해야 합니다.  
> Chrome 버전 확인: 주소창에 `chrome://version` 입력 → 첫 번째 숫자(예: **148**.0.7778.179 → `148`)

---

### 1. 담당 지역 크롤링

| 담당 | 명령어 |
|------|--------|
| 강남 | `python src/crawl_naver.py --area 강남` |
| 건대 | `python src/crawl_naver.py --area 건대` |
| 잠실 | `python src/crawl_naver.py --area 잠실` |

- 결과: `data/raw_reviews_naver.csv`
- 기본: 헤드리스 모드 (브라우저 창 안 뜸)
- 중단 후 재시작해도 `crawl_status_naver.csv` 기준으로 이어서 진행

#### 처음 실행할 때는 브라우저 띄워서 확인

```bash
python src/crawl_naver.py --show-browser --area 강남 --limit 3
```

> ⚠️ 네이버가 UI를 업데이트하면 리뷰가 0개로 수집될 수 있습니다.  
> 처음엔 `--show-browser`로 리뷰 텍스트가 제대로 잡히는지 확인하세요.

#### 특정 카테고리만

```bash
python src/crawl_naver.py --area 강남 --category 한식
```

---

### 2. 진행 상황 확인

```
[search] 강남 / 한식
[found] 80개 식당
[collect] 1/80 OO식당 (nv_12345678)
  → 리뷰 40개 수집
[collect] 2/80 XX식당 (nv_87654321)
  → 리뷰 18개 수집
[skip] 3/80 ZZ식당   ← 이미 완료된 식당
...
```

---

### 3. 수집 완료 후 디스코드 전달

1. `data/raw_reviews_naver.csv` 파일을 디스코드 채널에 업로드
2. 파일명에 지역명 포함 권장 (예: `raw_reviews_naver_강남.csv`)
3. 어느 지역인지 메시지로 같이 남겨주기

> **파일 크기 참고:** 지역 1개 최대 약 33,000개 리뷰 ≈ 6~7MB → 디스코드 25MB 제한 내 업로드 가능

---

## 현성이 작업 (취합 & 모델 재학습)

### 4. 파일 취합 & 병합

1. 디스코드에서 파일 3개 받아서 `data/` 폴더에 저장
   ```
   data/raw_reviews_naver_강남.csv
   data/raw_reviews_naver_건대.csv
   data/raw_reviews_naver_잠실.csv
   ```

2. 파일 3개를 하나로 합쳐서 `data/raw_reviews_naver.csv`로 저장  
   (엑셀/pandas로 합치거나, 아래처럼 Python으로 처리)
   ```python
   import pandas as pd
   files = [
       "data/raw_reviews_naver_강남.csv",
       "data/raw_reviews_naver_건대.csv",
       "data/raw_reviews_naver_잠실.csv",
   ]
   pd.concat([pd.read_csv(f) for f in files]).to_csv("data/raw_reviews_naver.csv", index=False)
   ```

3. 기존 카카오 데이터와 병합
   ```bash
   python src/merge_reviews.py
   ```
   `data/raw_reviews.csv`(기존 카카오) + `data/raw_reviews_naver.csv`(신규)를  
   하나의 `data/raw_reviews.csv`로 합칩니다. 중복 리뷰는 자동 제거됩니다.

   > **주의:** 기존 `raw_reviews.csv`를 덮어씁니다. 백업이 필요하면 먼저 따로 복사해두세요.

---

### 5. 모델 재학습

```bash
python src/build_features.py
python src/train_model.py
```

---

## 수집 규모

`config/sampling_plan.json` 기준:

| 항목 | 값 |
|------|-----|
| 지역 | 강남, 건대, 잠실 (3개) |
| 카테고리 | 11개 |
| 지역 × 카테고리 조합 | 33개 |
| 조합당 식당 수 | 80개 |
| 식당당 리뷰 목표 | 40개 |
| **최대 총 식당 수** | **2,640개** |
| **최대 총 리뷰 수** | **105,600개** |

> 식당의 실제 리뷰 수가 40개 미만이면 있는 만큼만 수집합니다.

---

## IP 차단 대응

### 왜 차단되나

네이버는 단일 원인이 아니라 여러 레이어로 봇을 탐지합니다.

| 레이어 | 내용 |
|--------|------|
| 요청 빈도 | 동일 IP에서 짧은 간격으로 반복 요청 시 차단 |
| 브라우저 핑거프린트 | headless Chrome은 Canvas/WebGL 렌더링이 일반 브라우저와 다름 |
| 행동 패턴 | 마우스 이동 없음, 균등한 딜레이, JS 직접 클릭 등 |
| 세션 이력 없음 | 쿠키·로그인 이력이 없는 신규 세션은 의심 대상 |
| IP 평판 | 이전 크롤링으로 오염된 IP는 CIDR 단위로 차단되기도 함 |

### 증상

```
[warn] IP 차단 감지 (211.221.205.72)
  → 리뷰 0개 수집
```

### 해결 방법

**1순위 — 핫스팟으로 IP 변경 (가장 빠름)**
```bash
# 스마트폰 핫스팟 연결 후 실행
python src/crawl_naver.py --show-browser --area 강남 --category 한식 --limit 3
```

**2순위 — VPN 사용**
- 국내 IP 제공 VPN 권장 (해외 IP는 오히려 더 빨리 차단됨)

**3순위 — 비헤드리스 모드**
- `--show-browser` 옵션으로 실제 브라우저 창을 띄워 실행
- headless보다 탐지율이 낮음

### 네트워크별 차단 위험도

| 네트워크 | 위험도 | 이유 |
|----------|--------|------|
| 학교 네트워크 | 🔴 거의 확실히 차단 | 수백~수천 명이 공인 IP 공유(NAT), 기존 크롤링으로 오염된 IP일 가능성 높음 |
| 가정용 IP (KT/SKT/LG U+) | 🟡 대체로 가능, 조건 있음 | 유동 IP라 차단 목록에 없는 경우 많음. 단, 단시간 대량 수집 시 차단 가능 |
| 모바일 핫스팟 | 🟢 안전 | 모바일 IP는 교체 주기가 짧아 차단 빠져나오기 쉬움 |

**가정용 IP 안전하게 쓰는 방법:**
- `--show-browser` 옵션 사용 (headless 대비 탐지율 낮음)
- 하루에 지역 1개씩 나눠서 실행 (한 번에 전체 돌리면 차단됨)
- 전체 2,640개 식당을 한 세션에 긁으려 하면 집 IP도 차단

### 주의사항

- IP가 차단된 상태에서는 코드를 아무리 수정해도 수집 불가
- `undetected-chromedriver`는 webdriver property 탐지만 우회 — IP 차단은 해결 안 됨
- 핫스팟도 장시간 연속 크롤링 시 차단될 수 있으므로 적절한 딜레이 유지

---

## 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| 리뷰 0개로 수집됨 | 네이버 UI 변경으로 CSS 선택자 불일치 | `--show-browser` + F12로 리뷰 텍스트 클래스명 확인 후 `_collect_visible_reviews()` 선택자 수정 |
| `[warn] IP 차단 감지` | 현재 IP가 네이버 pcmap에서 차단됨 | 핫스팟 또는 VPN으로 IP 변경 후 재실행 |
| `searchIframe` 없음 | 검색 결과 로드 지연 | 잠시 후 재시도 또는 `--show-browser` 확인 |
| place_id 추출 실패 | 네이버 URL 구조 변경 | `search_places()` 내 `_extract_place_id_from_url()` 정규식 확인 |
| `SessionNotCreatedException` | `version_main`과 Chrome 버전 불일치 | `chrome://version`에서 버전 확인 후 `make_driver()`의 `version_main` 수정 |
| `ModuleNotFoundError: distutils` | Python 3.12+ 환경 | `pip install setuptools` |
| 크롤링 도중 멈춤 | 네이버 로딩 지연 | 그냥 기다리거나 중단 후 재시작 (이어서 진행됨) |
| `OSError [WinError 6]` (종료 시) | uc 프로세스 정리 버그 | 무시해도 됨 — 수집 결과에 영향 없음 |

---

## 파일 구조

```
data/
├── raw_reviews.csv              ← 최종 병합 파일 (모델 학습에 사용)
├── raw_reviews_naver.csv        ← 네이버 수집 결과 (병합 전 원본)
├── crawl_status_naver.csv       ← 진행 상태 (재시작 기준)
└── crawl_errors_naver.csv       ← 에러 로그
```

---

## 참고: 수집 대상 카테고리

| 카테고리 | 검색 키워드 |
|---------|-----------|
| 한식 | 한식 |
| 일식 | 일식 |
| 중식 | 중식 |
| 양식 | 양식 |
| 술집 | 술집 |
| 치킨 | 치킨 |
| 분식 | 분식 |
| 이자카야 | 이자카야 |
| 고깃집 | 고깃집 |
| 호프/통닭 | 호프 |
| 카페 디저트 | 카페 |
